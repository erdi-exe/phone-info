# No pip packages required.
#
# Optional Termux packages:
#   pkg install termux-api     # phone helpers, Wi-Fi, clipboard, battery JSON
#   pkg install qrencode      # QR codes
#   pkg install traceroute    # traceroute tool
#
# Also install the Termux:API Android app from F-Droid.
